version: '3'
services:
#  api:
#    container_name: supplyx-api
#    depends_on: postgres
#    build: 
#      context: .
#      dockerfile: Dockerfile
#    ports:
#      - 3000:3000
#    environment:
#      POSTGRES_HOST: postgres:whybuyadmin123@postgres:5432
#      PORT: 3000
#    links:
#      - postgres

  postgres:
    container_name: postgres
    restart: always
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: SupplyX2022!
      POSTGRES_USER: supplyx
      POSTGRES_DB: supplyxDB
      PDDATA: /var/lib/postgressql/data
    volumes:
      - ./pgdata:/var/lib/postgressql/data

  pgadmin:
    container_name: pd-admin
    image: dpage/pgadmin4:4.18
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@supplyx
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_LISTEN_PORT: 3500
    ports:
      - "3500:3500"
    volumes:
      - ./pgadmin-data:/var/lib/pgadmin
    links:
      - "postgres:pgsql-server"